"""HTTP API package.

The active endpoints live in :mod:`app.api.v1`. The older ``app.api.api_v1``
tree is retained as an empty compatibility namespace and must not be mistaken
for the router currently mounted by :mod:`app.main`.
"""
