"""Legacy API-module placeholder.

SkyDeck's live router is assembled directly in :mod:`app.main` from modules
under :mod:`app.api.v1`. This file remains only so stale imports fail less
surprisingly while older integrations are removed.
"""
