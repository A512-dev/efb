"""Database infrastructure package.

``base`` defines the common SQLAlchemy declarative base, ``session`` creates
the engine and request-scoped sessions, and ``types`` contains PostgreSQL
column types not supplied by the standard SQLAlchemy type set.
"""
